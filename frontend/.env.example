# ============================================================
# SokoFlow POS - Environment Variables Template
# ============================================================
# Copy this to .env and fill in your actual keys.
# Never commit .env to git — only commit .env.example
#
# How to get each key:
#   PostHog:   https://app.posthog.com -> Project Settings -> API Keys
#   Mixpanel:  https://mixpanel.com -> Project Settings -> API Secret
#   Amplitude: https://app.amplitude.com -> Settings -> Projects -> API Key
#   Hotjar:    https://www.hotjar.com -> Settings -> Site ID (numeric)
#   Clarity:   https://clarity.microsoft.com -> Project -> Settings -> ID
# ============================================================

# ── PostHog (Product Analytics + Session Replay) ─────────────
VITE_POSTHOG_KEY=phc_REPLACE_WITH_YOUR_POSTHOG_API_KEY
VITE_POSTHOG_HOST=https://app.posthog.com
# For self-hosted PostHog:
# VITE_POSTHOG_HOST=https://your-posthog-instance.com

# ── Mixpanel (Funnel & Retention Analytics) ──────────────────
VITE_MIXPANEL_TOKEN=REPLACE_WITH_YOUR_MIXPANEL_TOKEN

# ── Amplitude (Behavioral Analytics) ─────────────────────────
VITE_AMPLITUDE_KEY=REPLACE_WITH_YOUR_AMPLITUDE_API_KEY

# ── Hotjar (Heatmaps & Session Recording) ────────────────────
# Used in index.html as a script tag — NOT read by Vite
# Add your numeric Hotjar ID directly to index.html
VITE_HOTJAR_ID=REPLACE_WITH_YOUR_HOTJAR_SITE_ID

# ── Microsoft Clarity (Free UX Tracking) ─────────────────────
# Used in index.html as a script tag — NOT read by Vite
# Add your Clarity Project ID directly to index.html
VITE_CLARITY_PROJECT_ID=REPLACE_WITH_YOUR_CLARITY_PROJECT_ID
